#ifndef __COMMAND_TYPE__
#define __COMMAND_TYPE__

/**
 * @brief Semantic commands decoded from incoming messages.
 */
enum class CommandType
{
    /// @brief Command to open the hangar door
    OPEN
};

#endif
